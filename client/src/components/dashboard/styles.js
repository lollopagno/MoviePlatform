import makeStyles from "@material-ui/core/styles/makeStyles";

//const red = '#fb7a74'
const useStyles = makeStyles((theme) => ({
    root: {
        flexGrow: 1,
    },
    account: {
        color: "#fcfcfc"
    }
}));

export {useStyles}
